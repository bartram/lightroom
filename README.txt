Install the DrupalPublish plugin in Adobe Lightroom

Edit the existing Drupal Publish settings, or create a new Drupal Publish service

Under Drupal Settings, set URL to the base path of your Drupal installation, with trailing slash (e.g. http://www.example.com/).

Publishing a collection in Lightroom will create a new Collection node.